trigger leadtriplet on Lead (before insert) {

    
    
 /*   list<lead> abctrippplet = new list<lead>();
    
    for(lead ll : trigger.new)
    {
        for(integer i =1 ;i<4;i++)
        {
            lead ll1 = new lead();
            ll1.lastname = 'lead copy' + i;
            ll1.Company = 'lead testing'+ ll.company+ 'copy';
            ll1.email =  i+ ll.Email;
            abctrippplet.add(ll1);
        }
    }
    if(TriggerDispatcher.recursive){
        TriggerDispatcher.recursive = false;
         if(abctrippplet.size()>0)
    {
        insert abctrippplet;
    }
    }
   */
    
}