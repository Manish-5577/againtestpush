trigger submitapprovalusingtrigger on Account (after update) {

    
 /*   if(trigger.isafter && trigger.isupdate){
     if(!future22.ff) {
       future22.ff = true;
      Submitaccountapproval.submitForApproval(trigger.new);
    }
        
    }*/
    
}