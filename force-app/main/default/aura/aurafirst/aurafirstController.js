({
	    doInit: function(cmp) {
        // Set the attribute value. 
        // You could also fire an event here instead.
       console.log('aura iniit');
    },
    onRender :function(cmp)
    {
        console.log('aura rendered!!!');
    }

})